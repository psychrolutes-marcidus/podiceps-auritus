[package]
name = "ferruginous"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
strip = true

[dependencies]
duckdb = { version = "1.4.4", features = ["loadable-extension", "vscalar"] }
libduckdb-sys = "1.4.4"

[lints]
workspace = true
